require 'spec_helper'

describe ScrapingHub do
  it 'has a version number' do
    expect(ScrapingHub::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
